# Use PyTorch CPU-only repository (saves ~3GB by avoiding CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
Pillow==10.1.0
numpy==1.26.2
opencv-python-headless==4.8.1.78

# PyTorch CPU-only (Python 3.9 compatible)
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# ML packages (Python 3.9 compatible - older builds may avoid execstack)
insightface==0.7.3
onnxruntime==1.12.1  # Older stable version for Python 3.9
easyocr==1.7.1

# Config & utilities
PyYAML==6.0.1
python-dotenv==1.0.0
aiofiles==23.2.1

# Liveness detection dependencies
mediapipe>=0.10.0,<0.11.0
imutils>=0.5.3,<0.6.0

